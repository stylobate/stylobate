stylobate_defaults('kind-button', '_reset')

kind-button($this)
  kind-pill()
  display: -moz-inline-box // There are, like, A LOT of problems in Fx fixed by this

  box-sizing: border-box

  cursor: pointer
  user-select: none

kind-button_reset($this)
  reset('button')
