kind-button()
  +cache('kind-button')
    kind-pill()

    cursor: pointer
    user-select: none
    vertical-align: baseline

  $align = utilus_retrieve(arguments, middle top bottom)
  vertical-align: $align if $align

  unless no-reset in arguments
    reset('button')
