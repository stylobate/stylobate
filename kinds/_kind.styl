remove_first($where)
  $result = unquote('')
  for $item, $index in $where
    push($result, $item) if $index > 0
  return $result

kind()
  if is_hash(arguments)
    for $kind in arguments
      kind-{$kind[0]}(remove_first($kind))
  else
    kind-{arguments[0]}(remove_first(arguments))
