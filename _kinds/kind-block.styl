kind-block()
  position: retrieve(arguments, static, relative)

  if inline in arguments
    display: inline-block
    vertical-align: retrieve(arguments, middle baseline bottom, top)
  else
    display: block
